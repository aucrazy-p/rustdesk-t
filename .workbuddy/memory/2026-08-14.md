# RustDesk 项目工作日志

## 2026-08-14 托盘隐藏 + CM强制隐藏 + Windows便携版CI

### GitHub 仓库与CI产物
- 仓库: https://github.com/aucrazy-p/rustdesk-t (公开)
- CI工作流: .github/workflows/windows-portable.yml (复用 bridge.yml + third-party-RustDeskTempTopMostWindow.yml)
- 首次构建成功 (run #31804220666, artifact id 9221872633)
- artifact名称: rustdesk-windows-x64-portable (30.8MB)
- GitHub Token 已用于推送与下载,用完应去settings/tokens删除
- 已通过API下载artifact并双层解压到桌面:
  - C:/Users/aucrazy/Desktop/rustdesk-1.4.9-windows-x64-portable.zip (发客户的压缩包)
  - C:/Users/aucrazy/Desktop/rustdesk-1.4.9-windows-x64-portable/rustdesk.exe (本地测试用)
- 下载方式: https://github.com/aucrazy-p/rustdesk-t/actions -> 最新成功run -> 拉到底部Artifacts -> 点击rustdesk-windows-x64-portable

### 本地编译环境检测结论(未安装,等用户指令)
- 已有: VS2022 BuildTools(MSVC14.44+WinSDK10.0.26100), Rust1.97.1 stable(msvc), vcpkg(2026-07版), Flutter(F:/dev/flutter,2025-12新版), Python3.13, git, Docker29.0.1(daemon未运行)
- 缺口: Flutter需3.24.5+RustDesk自定义引擎+补丁; vcpkg需checkout到锁定commit 120deac; Rust1.75工具链缺(可先试1.97); generated_bridge.dart需CI artifact解压或装flutter_rust_bridge_codegen; F:/dev/flutter有残留锁文件需删
- 完成度约60%,补齐后首次约30-60分钟,以后改Dart代码2-3分钟出新包

### 完成的修改

#### 1. 主窗口点 X 和最小化都隐藏到托盘
- 文件: `flutter/lib/desktop/pages/connection_page.dart`
- 在 `onWindowClose()` 中追加 `windowManager.hide()` (第292行)
- 在 `onWindowEvent('minimize')` 中追加 `windowManager.hide()` (第261行)
- 主窗口已设 `setPreventClose(true)` (main.dart:112)，点X不退出，现在改为隐藏到托盘
- 点最小化按钮也直接隐藏到托盘，不缩到任务栏
- 托盘图标由 `src/tray.rs` 的 `--tray` 进程管理，Open 菜单可恢复窗口

#### 2. 连接管理窗口(CM)强制隐藏
- 文件: `flutter/lib/main.dart`
- 新增常量 `kForceHideCm = true` (第42行)
- `runConnectionManagerScreen()` 改为强制 `hideCm=true` + `hideCmWindow(isStartup:true)`
- `showCmWindow()` 开头检查 `kForceHideCm`，为true则直接return（空操作）

#### 3. 连接过程不弹CM窗口
- 无需额外文件修改，由需求2自动覆盖
- server_model.dart 中所有 `if(!hideCm) showCmWindow()` 判断均为false
- chat_model.dart 的 `showCmWindow()` 直接调用也被空操作拦截

#### 4. GitHub Actions Windows便携版构建工作流
- 新建文件: `.github/workflows/windows-portable.yml`
- 复用已有可复用工作流: `bridge.yml` (生成桥接文件) + `third-party-RustDeskTempTopMostWindow.yml` (x64)
- 构建命令: `python3 build.py --portable --flutter --skip-portable-pack --hwcodec --vram`
- 产物: `rustdesk-1.4.9-windows-x64-portable.zip`
- 触发: workflow_dispatch + push to main/master
- 关键依赖: Rust 1.75, Flutter 3.24.5, LLVM 15, vcpkg(commit 120deac), rustdesk自定义引擎

### 关键代码路径备忘
- 主窗口关闭: connection_page.dart onWindowClose() -> main.dart setPreventClose(true)
- 主窗口最小化: connection_page.dart onWindowEvent('minimize') -> windowManager.hide()
- CM启动: main.dart runConnectionManagerScreen() (由 --cm 参数触发)
- CM显示/隐藏: main.dart showCmWindow()/hideCmWindow() + server_model.dart 各处调用
- 托盘: src/tray.rs make_tray() (由 --tray 参数触发，独立进程)
- 构建: build.py --portable --flutter --skip-portable-pack
- 授权模式: password_security.rs approve_mode() 默认Both, 有有效密码时自动接受
  - connection.rs:2915 仅当Click模式或Both无密码时才弹授权对话框

#### 5. 推送到 GitHub 并触发 CI 构建
- 创建仓库: github.com/aucrazy-p/rustdesk-t (公开)
- git init + 845 文件提交 + push 到 main 分支
- 工作流 "Build Windows Portable" 已自动触发 (run #31804220666)
- 依赖的可复用工作流均已确认存在: bridge.yml, third-party-RustDeskTempTopMostWindow.yml
- patches 目录已确认存在: flutter_3.24.4_dropdown_menu_enableFilter.diff
- Token 已从 remote URL 中清除 (安全)
- 构建预计 20-30 分钟，产物为 Artifact: rustdesk-windows-x64-portable
